// Function to calculate and return the sum of two numbers which is also a number.
function sum(a: number, b: number): number {
    
    return a + b;
}

// Call the sum function with two numbers and store the result in the variable 'ans'
let ans: number = sum(10, 20);

console.log(ans); 

// ts compiler does not check your logical error.